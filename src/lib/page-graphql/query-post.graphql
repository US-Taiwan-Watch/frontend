query EditorPageQuery($articleId: String!) {
  article(id: $articleId) {
    id
    title
    content
    slug
    preview
    isPublished
    authors
    pusblishTime
    createdTime
    lastModifiedTime
    imageSource
    tags
  }
  editors {
    id
    name
  }
}