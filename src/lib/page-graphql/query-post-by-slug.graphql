query PublicPost($slug: String!) {
  publicArticle(slug: $slug) {
    id
    title
    content
    slug
    preview
    isPublished
    authors
    pusblishTime
    createdTime
    lastModifiedTime
    imageSource
    tags
  }
}