query PublicPost($slug: String!) {
  publicArticle(slug: $slug) {
    id
    title
    content
    slug
    preview
    isPublished
    pusblishTime
    createdTime
    lastModifiedTime
    imageSource
    tags
    authorInfos {
      id
      name
      picture
      nickname
    }
  }
}